# thiagowfx/homebrew-taps

This repository hosts custom Homebrew formulae for the `thiagowfx/taps` tap. To add the tap:

```bash
brew tap thiagowfx/taps
```

Formulas should live under `Formula/` following the standard Homebrew structure.
